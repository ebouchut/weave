[package]
name = "weave-driver"
version = "0.1.0"
edition = "2021"

[dependencies]
weave-core = { path = "../weave-core" }
sem-core = { path = "../../../sem/crates/sem-core" }
weave-crdt = { path = "../weave-crdt", optional = true }
env_logger = "0.11"
log = "0.4"

[features]
crdt = ["weave-crdt"]
